import { Subject } from 'rxjs';

export interface InputHolder {
    group? : string;
    demout? : string;
    helpPath?: string;
    showCaseFlag? : string;
    parentClick?: boolean;  
    imagesList?: string;
}

