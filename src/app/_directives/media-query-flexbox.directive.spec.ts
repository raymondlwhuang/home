import { MediaQueryFlexboxDirective } from './media-query-flexbox.directive';

describe('MediaQueryFlexboxDirective', () => {
  it('should create an instance', () => {
    const directive = new MediaQueryFlexboxDirective();
    expect(directive).toBeTruthy();
  });
});
